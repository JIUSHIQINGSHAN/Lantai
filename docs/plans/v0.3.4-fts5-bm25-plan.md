# Remembrance-System v0.3.4 FTS5 接入 + BM25 缓存执行方案

**生成日期**: 2026-08-03
**依据**: [ADR-0008](../adr/0008-fts5-parallel-recall.md)（FTS5 并列接入，已 Accepted）+ 审计 M4（BM25 每次全量重建）
**适用执行者**: 执行力强、推理弱的模型。**所有设计决策已完成，禁止提问，按步骤直接执行。**

---

## 技能调用流程

```
[已完成] ADR-0008 定案：FTS5 并列接入（权重 0.6/0.25/0.05/0.1，同事务强一致）
[已完成] v0.3.3 P1 安全收口（SSRF/备份/MCP）→ 110 passed / 2 failed
[本方案] v0.3.4：FTS5 写入同步 + 检索融合 + BM25 缓存
[之后]   test_prefilter 收尾 → 供应链(M6) → reranker 端点(M7)
```

**给便宜模型的开场白（复制即用）**：

> 阅读 `C:\Users\Asus\Desktop\记忆\docs\plans\v0.3.4-fts5-bm25-plan.md`，从 Wave 0 开始按顺序执行到 Wave 4。所有代码已写死，禁止提问，禁止自由发挥。

---

## 执行者须知（必读）

1. 本方案已做完所有设计决策。**禁止向用户提问**，按 Wave 0→4 顺序直接执行。
2. 每个 Wave 结束后运行验收命令，通过后再 `git commit`。
3. 若某步骤验收失败：用 `git checkout -- <file>` 回滚该步骤改动的文件，标记 FAILED，继续执行无依赖的后续步骤。
4. 禁止删除既有测试；禁止放宽既有断言；禁止修改 `.env`。
5. Python 环境：复用 `C:\Users\Asus\Desktop\记忆\.venv-audit\Scripts\python.exe`。
6. 项目根目录：`C:\Users\Asus\Desktop\记忆`。**所有 git/pytest 命令必须先 `cd` 到项目根目录**。pytest 收集报 `OpenAIError` 时设 `OPENAI_API_KEY=sk-dummy-audit`。
7. 当前 HEAD 应为 `b9a9161`。
8. **禁止操作项目根目录的 `remembrance.db`**（用户运行数据）。FTS 表迁移由 `init_fts` 自动完成（见 Wave 1 步骤 1），无需手工 DROP。

---

## Wave 0：前置校验（只读）

```bash
cd "C:\Users\Asus\Desktop\记忆"
git log --oneline -1          # 期望：b9a9161
.\.venv-audit\Scripts\python.exe -m pytest tests/ -q --tb=no
```

记录基线。**期望：110 passed / 2 failed**。若不同，记录实际值后继续。

---

## Wave 1：FTS 写入同步（同事务强一致，ADR-0008 决策 3）

### 步骤 1：`remembrance/storage/fts.py` 整体替换为

```python
"""
FTS5 全文搜索：trigram 分词器 + 同事务写入同步（ADR-0008）

- init_fts：建表；检测到旧 schema（无 memory_id 列）自动 DROP 重建（旧表从无数据，无损失）
- sync_fts：在调用方的 SQLAlchemy 事务内同步索引（强一致）
- search_fts：子串召回
"""
import sqlite3

from remembrance.core.logger import logger


def init_fts(conn: sqlite3.Connection):
    """初始化 FTS5 虚拟表；自动迁移旧 schema。"""
    try:
        cols = [r[1] for r in conn.execute("PRAGMA table_info(memory_fts)").fetchall()]
        if cols and "memory_id" not in cols:
            conn.execute("DROP TABLE memory_fts")
            logger.warning("legacy memory_fts schema detected, dropped for recreation")
        conn.execute("""
            CREATE VIRTUAL TABLE IF NOT EXISTS memory_fts USING fts5(
                memory_id UNINDEXED,
                content,
                tokenize='trigram'
            )
        """)
        conn.commit()
        logger.info("FTS5 + trigram initialized")
    except Exception as e:
        logger.warning("FTS5 init failed: %s", e)


def sync_fts(session, memory_id: str, content: str | None) -> None:
    """同事务同步 FTS 索引（ADR-0008：强一致，不吞异常）。

    content 非 None：先删后插（UPSERT 语义）；
    content 为 None：删除该记忆的 FTS 行。
    """
    from sqlalchemy import text
    conn = session.connection()
    conn.execute(text("DELETE FROM memory_fts WHERE memory_id = :id"),
                 {"id": memory_id})
    if content:
        conn.execute(
            text("INSERT INTO memory_fts(memory_id, content) VALUES (:id, :content)"),
            {"id": memory_id, "content": content})


def index_fts(conn: sqlite3.Connection, memory_id: str, content: str):
    """索引单条（独立连接场景；生产路径用 sync_fts，此函数仅供测试/脚本）。"""
    try:
        conn.execute(
            "INSERT INTO memory_fts(memory_id, content) VALUES (?, ?)",
            (memory_id, content)
        )
        conn.commit()
    except Exception as e:
        logger.warning("FTS5 index failed for %s: %s", memory_id, e)


def search_fts(conn: sqlite3.Connection, query: str, top_k: int = 5) -> list[str]:
    """FTS5 全文搜索，返回匹配的记忆 ID 列表。"""
    try:
        keywords = [w.strip() for w in query.split() if w.strip()]
        if not keywords:
            return []
        match_query = " AND ".join(keywords)
        cursor = conn.execute(
            "SELECT memory_id FROM memory_fts WHERE content MATCH ? ORDER BY rank LIMIT ?",
            (match_query, top_k)
        )
        return [row[0] for row in cursor.fetchall()]
    except Exception as e:
        logger.warning("FTS5 search failed: %s", e)
        return []
```

### 步骤 2：`remembrance/evolution/promoter.py` 四处接入

顶部 import 追加：

```python
from remembrance.storage.fts import sync_fts
```

（a）`apply_proposal` add 分支——`index_memory_item(mem.id, emb, {...})` 行**之后**追加：

```python
            sync_fts(s, mem.id, mem.content)
```

（b）`apply_proposal` update 分支——`index_memory_item(existing.id, emb, {...})` 行**之后**追加：

```python
            sync_fts(s, existing.id, existing.content)
```

（c）`rollback`——`_make_checkpoint(s, mem, before, proposal_id="", trigger="rollback")` 行**之后**追加：

```python
        sync_fts(s, mem.id, mem.content)
```

（d）`delete_memory`——`s.delete(mem)` 行**之后**追加：

```python
            sync_fts(s, memory_id, None)
```

### 步骤 3：`remembrance/core/settings.py` 追加检索权重配置（零硬编码，ADR-0002）

在 `BACKUP_MANIFEST_VERSION` 之后追加：

```python
    # 混合检索权重（ADR-0008）
    RETRIEVAL_W_VECTOR: float = 0.6
    RETRIEVAL_W_BM25: float = 0.25
    RETRIEVAL_W_FTS: float = 0.05
    RETRIEVAL_W_DECAY: float = 0.1
    FTS_RECALL_TOP_K: int = 20  # FTS 子串召回上限
```

### 验收

```bash
.\.venv-audit\Scripts\python.exe -m pytest tests/test_p0.py -q     # 期望 7 passed（fts 层仍绿）
.\.venv-audit\Scripts\python.exe -m pytest tests/test_e2e.py -q    # 期望 18 passed
```

### 提交

```bash
git commit -am "feat(v0.3.4): FTS5 write-sync in promoter txn (ADR-0008), auto-migrate legacy fts schema, retrieval weights in settings"
```

---

## Wave 2：检索融合 + BM25 缓存（M4）

### 步骤 1：`remembrance/retrieval/hybrid.py`

（a）文件顶部 import 区追加：

```python
from remembrance.storage.fts import search_fts
```

（b）模块级追加 BM25 缓存（放在 `hybrid_search` 定义之前）：

```python
# BM25 语料缓存（M4）：key = items 的 (id, content) 有序元组，命中即复用
_BM25_CACHE: dict = {"key": None, "bm25": None}


def _get_bm25(items: list) -> "BM25Okapi":
    key = tuple((m.id, m.content) for m in items)
    if _BM25_CACHE.get("key") == key:
        return _BM25_CACHE["bm25"]
    corpus = [jieba.lcut(m.content) for m in items]
    bm25 = BM25Okapi(corpus)
    _BM25_CACHE["key"] = key
    _BM25_CACHE["bm25"] = bm25
    return bm25
```

（c）`# Step 3: BM25 + 向量距离 + 衰减 混合打分` 整段（第 104–122 行）替换为：

```python
    # Step 3: FTS5 子串召回（ADR-0008）
    fts_hits: set[str] = set()
    try:
        with db.get_session() as s:
            fts_hits = set(search_fts(
                s.connection().connection.driver_connection,
                query, top_k=settings.FTS_RECALL_TOP_K))
    except Exception:
        fts_hits = set()  # FTS 不可用不影响主检索

    # Step 4: BM25（带缓存，M4）+ 向量距离 + FTS 命中 + 衰减 融合打分
    distances = {r["id"]: r["distance"] for r in vector_results}
    bm25 = _get_bm25(items)
    bm_scores = bm25.get_scores(jieba.lcut(query))
    # 不用 ndarray.ptp()（numpy>=2 已移除），min/max 兼容各版本
    bm_range = bm_scores.max() - bm_scores.min()
    bm_norm = (bm_scores - bm_scores.min()) / (bm_range + 1e-8)

    scored_items = []
    for i, m in enumerate(items):
        vs = 1.0 - distances.get(m.id, 1.0)
        lane = getattr(m, "lane", "general") or "general"
        lane_boost = settings.LANE_RETRIEVAL_BOOST.get(lane, 1.0)
        fts_hit = 1.0 if m.id in fts_hits else 0.0
        score = (settings.RETRIEVAL_W_VECTOR * vs
                 + settings.RETRIEVAL_W_BM25 * float(bm_norm[i])
                 + settings.RETRIEVAL_W_FTS * fts_hit
                 + settings.RETRIEVAL_W_DECAY * m.decay_score) * lane_boost
        scored_items.append((score, m))

    # 追加召回：FTS 命中但未进向量候选的 active 记忆（并列召回，ADR-0008 决策 1）
    ranked_ids = {m.id for _, m in scored_items}
    extra_ids = fts_hits - ranked_ids
    if extra_ids:
        with db.get_session() as s:
            extra = s.exec(select(MemoryItem)
                           .where(MemoryItem.id.in_(extra_ids),
                                  MemoryItem.status == "active")).all()
        for m in extra:
            scored_items.append(
                (settings.RETRIEVAL_W_FTS + settings.RETRIEVAL_W_DECAY * m.decay_score, m))

    scored_items.sort(key=lambda x: -x[0])
    candidates = scored_items[:fetch_n]
```

（d）`# Step 4: Reranker（可选）` 注释改为 `# Step 5: Reranker（可选）`（仅注释编号，逻辑不动）。

注意：`db` 已在 hybrid.py 顶部导入（`from remembrance.storage import db`），`select` 已导入，`MemoryItem` 已导入，`settings` 已导入——无需新增 import。

### 步骤 2：冒烟验证融合不炸

```bash
cd "C:\Users\Asus\Desktop\记忆"
REMEMBRANCE_HOME="C:/Users/Asus/WorkBuddy/2026-07-27-11-56-23/.workbuddy/audit-runtime/v34w2" OPENAI_API_KEY="sk-dummy-audit" ./.venv-audit\Scripts\python.exe -c "
from remembrance.retrieval import hybrid
print('hybrid import OK:', hasattr(hybrid, '_get_bm25'))
"
```

### 验收

```bash
.\.venv-audit\Scripts\python.exe -m pytest tests/test_features.py -q     # 期望 15 passed（trace 测试覆盖新打分路径）
.\.venv-audit\Scripts\python.exe -m pytest tests/test_e2e.py -q          # 期望 18 passed
.\.venv-audit\Scripts\python.exe -m pytest tests/test_p0.py -q           # 期望 7 passed
```

### 提交

```bash
git commit -am "feat(v0.3.4): hybrid search FTS5 fusion + extra recall (ADR-0008), BM25 corpus cache (M4)"
```

---

## Wave 3：集成测试 + 文档

### 步骤 1：新建 `tests/test_fts_integration.py`

```python
"""FTS5 集成测试：同事务同步 + 检索融合 + 追加召回 + BM25 缓存"""
from unittest.mock import Mock, patch

import pytest
from sqlalchemy.pool import StaticPool
from sqlmodel import SQLModel, Session, create_engine

import remembrance.storage.db as db_module
from remembrance.core.ids import new_id
from remembrance.core.time import utcnow
from remembrance.models.tables import MemoryItem
from remembrance.storage.fts import init_fts, search_fts, sync_fts


@pytest.fixture
def engine():
    e = create_engine("sqlite:///:memory:",
                      connect_args={"check_same_thread": False},
                      poolclass=StaticPool)
    SQLModel.metadata.create_all(e)
    init_fts(e.raw_connection())
    return e


def _add_mem(engine, content: str) -> str:
    mid = new_id("mem")
    with Session(engine) as s:
        s.add(MemoryItem(
            id=mid, memory_type="general", key=mid, content=content,
            lane="general", status="active", importance=0.5, use_count=0,
            decay_score=1.0, last_used_at=utcnow(), created_at=utcnow()))
        s.commit()
    return mid


def test_sync_fts_same_transaction(engine):
    """sync_fts 与记忆写入同事务：commit 后 FTS 可见"""
    mid = new_id("mem")
    with Session(engine) as s:
        s.add(MemoryItem(
            id=mid, memory_type="general", key=mid, content="用户喜欢喝咖啡",
            lane="general", status="active", importance=0.5, use_count=0,
            decay_score=1.0, last_used_at=utcnow(), created_at=utcnow()))
        sync_fts(s, mid, "用户喜欢喝咖啡")
        s.commit()
    with engine.connect() as conn:
        assert mid in search_fts(conn.connection.driver_connection, "咖啡")


def test_sync_fts_update(engine):
    """同 id 再同步 = 覆盖（先删后插）"""
    mid = _add_mem(engine, "旧内容")
    with Session(engine) as s:
        sync_fts(s, mid, "新内容")
        s.commit()
    with engine.connect() as conn:
        c = conn.connection.driver_connection
        assert mid in search_fts(c, "新内容")
        assert mid not in search_fts(c, "旧内容")


def test_sync_fts_delete(engine):
    """content=None 删除索引"""
    mid = _add_mem(engine, "要删除的记忆")
    with Session(engine) as s:
        sync_fts(s, mid, None)
        s.commit()
    with engine.connect() as conn:
        assert mid not in search_fts(conn.connection.driver_connection, "记忆")


def test_hybrid_fts_extra_recall(engine):
    """FTS 命中但向量未命中的记忆被追加召回"""
    from remembrance.retrieval import hybrid

    vec_hit_id = _add_mem(engine, "向量命中的记忆")
    fts_only_id = _add_mem(engine, "咖啡因摄入记录")
    # 向量只返回 vec_hit；fts 用真实表
    with Session(engine) as s:
        sync_fts(s, fts_only_id, "咖啡因摄入记录")
        sync_fts(s, vec_hit_id, "向量命中的记忆")
        s.commit()

    def get_test_session():
        return Session(engine)

    def fake_store(ids):
        return Mock(search=Mock(return_value=[
            {"id": i, "distance": 0.3, "metadata": {}} for i in ids]))

    with patch.object(db_module, "get_session", get_test_session), \
         patch("remembrance.retrieval.intent.chat_json",
               return_value={"intent": "fact_lookup", "reason": "test"}), \
         patch("remembrance.retrieval.hybrid.embed", return_value=[[0.1] * 8]), \
         patch("remembrance.retrieval.hybrid.get_vector_store",
               return_value=fake_store([vec_hit_id])):
        results = hybrid.hybrid_search("咖啡因", top_k=5, use_rerank=False)
    ids = {r["memory"]["id"] for r in results}
    assert vec_hit_id in ids
    assert fts_only_id in ids  # 追加召回生效
```

注意：
- `test_hybrid_fts_extra_recall` 中，`hybrid.hybrid_search` 内部通过 `db.get_session()`（已被 patch）读 FTS——FTS 表与 MemoryItem 同内存库，天然一致。
- 若 `s.connection().connection.driver_connection` 取到的是 `sqlite3.Connection`（是），`search_fts` 直接可用。
- 若 `test_hybrid_fts_extra_recall` 因 `get_test_session` 被 `with ... as s` 上下文管理（hybrid 里 `with db.get_session() as s:`）——`Session` 支持上下文管理器，正常。

### 步骤 2：`docs/aidumem-port-results.md` 测试表追加一行

| `test_fts_integration.py` | 4 | ✅ 全绿（v0.3.4 新增，FTS 同步与融合） |

并将 `**总计**` 行更新为实际数字（原 112 + 4 = **116**；若其他行有变化按实测）。

### 验收

```bash
.\.venv-audit\Scripts\python.exe -m pytest tests/test_fts_integration.py -q   # 期望 4 passed
```

### 提交

```bash
git commit -am "test(v0.3.4): FTS5 integration tests - txn sync/update/delete, extra recall; update results doc"
```

---

## Wave 4：终验

```bash
cd "C:\Users\Asus\Desktop\记忆"
git log --oneline -4            # 确认 3 个 Wave 提交
.\.venv-audit\Scripts\python.exe -m pytest tests/ -q --tb=short
```

**期望：全量 114 passed / 2 failed（116 个测试）**；新增 test_fts_integration 4 个必须全绿。

向用户输出执行报告：各 Wave 状态（DONE/FAILED + commit hash）、最终测试数字、残留事项。

---

## 附：本方案不做的事（防止执行者越界）

- 不修 `test_prefilter.py` 剩余 2 个失败（另立项）。
- 不做 reranker 端点白名单/独立密钥（M7）、供应链（M6）——另立项。
- 不改 gate 决策、遗忘逻辑、`_lane_strength` 数值。
- 不操作项目根的 `remembrance.db`（迁移由 init_fts 自动完成）；不清理 Git 历史；不轮换密钥。
- 不改 trace 输出结构（保留 intent/vector_search/decay_filter/rerank/final 各步）。
