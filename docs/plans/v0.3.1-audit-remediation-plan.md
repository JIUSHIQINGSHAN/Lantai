# Remembrance-System v0.3.1 审计修复执行方案

**生成日期**: 2026-08-02
**依据**: `docs/aidumem-port-results.md` 独立审计报告（总体 ⚠️ 有条件通过）
**目标**: 修复全部 7 项 P1 与 12 项 P2 问题，使 v0.3.0 移植达到"通过"评级
**执行者须知（必读）**:

1. 本方案已做完所有设计决策。**禁止向用户提问**，按步骤直接执行。
2. 按 Wave 0→7 顺序执行；每个 Wave 结束后运行其指定的测试命令，通过后再 `git commit`。
3. 若某步骤验收失败：用 `git checkout -- <file>` 回滚该步骤改动的文件，在最终报告中将该步骤标记为 FAILED，继续执行与之无依赖的后续步骤；依赖失败步骤的后续步骤标记 SKIPPED。
4. 禁止修改 `.env`；禁止删除任何既有测试；禁止放宽既有断言来"让测试通过"。
5. Python 环境：优先复用工作区已有的 `.venv-audit\Scripts\python.exe`（依赖已装齐）；若不存在，用 `C:\Users\Asus\AppData\Local\Programs\Python\Python312\python.exe -m venv .venv` 创建并 `pip install -e .` + `pip install jieba pytest httpx requests`。下文所有 `python` 均指该 venv 解释器。
6. 项目根目录：`C:\Users\Asus\Desktop\记忆`。所有路径相对于此。
7. 执行前先读 `AGENTS.md` 与 `docs/agents/*.md`，遵守本仓库 issue tracker 约定。

---

## Wave 0：前置校验（只读）

1. `git log --oneline -1` 确认 HEAD ≥ d579d35；`git status --short` 确认工作区干净（`.venv-audit/`、`.audit-baseline/`、`docs/audit-prompt.md` 为已知未跟踪项，忽略）。
2. `python -m pytest tests/ -q --tb=no` 记录基线：**期望 81 passed / 6 failed**（6 个失败为预存 bug：`test_p0.py` 4 个 + `test_prefilter.py::TestGateContentQuery::test_short_query` 和 `TestGateDefault::test_random`）。若失败数 ≠ 6 或通过数 ≠ 81，停止并在报告中说明实际数字，不要继续。

---

## Wave 1：打包与配置修复（F1–F4）

### F1 修复 pyproject.toml（P1：克隆即跑破裂 + 版本不一致）

文件：`pyproject.toml`

- 第 3 行 `version = "0.2.0"` → `version = "0.3.1"`
- dependencies 列表追加两项：`"jieba>=0.42"`、`"requests>=2.31"`

验收：`python -m pip install -e . -q && python -c "from remembrance.retrieval import hybrid; print('OK')"` 输出 `OK`。

### F2 validate_config 补充 API_KEY 空警告（P2，ADR-0002 决策第 4 条）

文件：`remembrance/core/settings.py`，在 `validate_config()` 的第二个 if 之后追加：

```python
    if not settings.API_KEY:
        logger.warning("API_KEY 为空：API 将以无鉴权模式运行（仅建议本机开发）")
```

验收：`python -m pytest tests/test_settings.py -q` 全过（14 个）。

### F3 修复 backup.py 目录命名 bug（P2）

文件：`scripts/backup.py` 第 32 行：

- 旧：`dest_dir = settings.REMEMBRANCE_HOME / "backups" / f"backup_{settings.__class__.__name__}"`
- 新：`dest_dir = settings.REMEMBRANCE_HOME / "backups" / f"backup_{datetime.now():%Y%m%d_%H%M%S}"`

验收：`python -c "from datetime import datetime; print(f'backup_{datetime.now():%Y%m%d_%H%M%S}')"` 正常输出；`python scripts/backup.py --dry-run` 退出码 0 且打印含 `backup_2026` 的目录名。

### F4 修复 Dockerfile builder 空 wheel（P2）

文件：`Dockerfile`，builder 阶段改为先拷源码再构建：

```dockerfile
FROM python:3.11-slim AS builder
WORKDIR /build
ENV PIP_NO_CACHE_DIR=1 PIP_DISABLE_PIP_VERSION_CHECK=1
COPY pyproject.toml ./
COPY remembrance/ ./remembrance/
RUN pip install --upgrade pip build && python -m build --wheel
```

runtime 阶段改为安装 wheel 而非 COPY 源码（删除 `COPY remembrance/ ./remembrance/`，保留 `COPY api_server.py ./`）：

```dockerfile
COPY --from=builder /wheels /wheels
RUN pip install --no-cache-dir /wheels/*.whl
COPY api_server.py ./
```

验收：`python -c "import dockerfile_parse"` 不做要求（无 docker 环境）；改为人工核对 Dockerfile 全文满足：builder 含 `COPY remembrance/ ./remembrance/`、runtime 不含该行、HEALTHCHECK 与 VOLUME 保持原样。若有 docker 环境则 `docker build -t remembrance:test .` 成功。

**Wave 1 提交**: `git add -A && git commit -m "fix(v0.3.1): pyproject deps+version, validate_config API_KEY warn, backup timestamp dir, Dockerfile real wheel"`

---

## Wave 2：功能接线（F5–F8）

### F5 修复 fastpath 链路断裂（P1，US-12）

根因：`memory_service.py:66` 创建 `status="fastpath"` 的候选，但 `evolve_worker.py:15` 只捞 `status=="new"`，fastpath 候选永远不进闸门。

文件：`remembrance/workers/evolve_worker.py` 第 14–15 行：

- 旧：`.where(MemoryCandidate.status == "new")`
- 新：`.where(MemoryCandidate.status.in_(["new", "fastpath"]))`

同时在 `remembrance/models/tables.py` 的 `MemoryCandidate` 类 docstring 处（第 23 行类定义上方无注释，直接在类下加一行类注释，可选）不改；**仅改 evolve_worker 一行**。

验收：`python -m pytest tests/test_features.py -q` 全过（13 个）。

### F6 接入三态去重（P1，US-26/27/28，T10）

**步骤 1**——扩展 `remembrance/gate/dedup.py` 的 `find_similar` 返回相似度（该函数当前零调用方，改签名安全）：

```python
def find_similar(session, query_results: list[dict]) -> tuple[str, MemoryItem | None, float]:
    """对候选与现有 active 记忆做三态判定。

    返回 (action, target_memory_or_None, best_sim)。
    action: "merge" | "update" | "insert"
    """
    best_sim = 0.0
    best_mem = None
    for r in query_results:
        mem = session.get(MemoryItem, r["id"])
        if not mem or mem.status != "active":
            continue
        sim = 1.0 - r["distance"]  # cosine 距离 → 相似度
        if sim > best_sim:
            best_sim = sim
            best_mem = mem

    if best_mem is None:
        return "insert", None, 0.0
    if best_sim >= settings.DEDUP_MERGE_THRESHOLD:
        return "merge", best_mem, best_sim
    if best_sim >= settings.DEDUP_UPDATE_THRESHOLD:
        return "update", best_mem, best_sim
    return "insert", None, best_sim
```

**步骤 2**——`remembrance/services/memory_service.py` 接入。文件顶部 import 追加：

```python
from remembrance.gate.dedup import find_similar
from remembrance.models.tables import MemoryItem, MemoryCandidate, RawDocument, MemoryProposal
```

（`MemoryProposal` 并入既有 tables import 行即可。）新增私有函数：

```python
def _apply_dedup(s, title: str, content: str, lane: str) -> dict | None:
    """candidate 创建前的三态去重。返回 None 表示 insert（继续正常建候选）。"""
    vec_results = vector_store.search(content, top_k=1)
    action, target, sim = find_similar(s, vec_results)
    if action == "insert" or target is None:
        return None
    if action == "merge":
        target.last_accessed = utcnow()
        target.importance = min(1.0, target.importance + 0.1)
        s.add(target)
        s.commit()
        return {"dedup_action": "merge", "target_memory_id": target.id, "similarity": round(sim, 4)}
    prop = MemoryProposal(
        memory_id=target.id,
        proposal_type="update",
        payload={"title": title, "content": content, "lane": lane},
        confidence=round(sim, 4),
        status="pending",
    )
    s.add(prop)
    s.commit()
    s.refresh(prop)
    return {"dedup_action": "update", "target_memory_id": target.id,
            "proposal_id": prop.id, "similarity": round(sim, 4)}
```

**步骤 3**——在两个候选创建点调用。`_create_candidate_with_extraction`：在 `cand = MemoryCandidate(...)` 之前插入：

```python
    dedup_result = _apply_dedup(s, title, content, lane)
    if dedup_result is not None:
        return dedup_result
```

`_create_candidate_direct`：同样在 `cand = MemoryCandidate(...)` 之前插入相同两行。注意：两个函数当前的返回语句 `return {"document_id": ..., ...}` 在 `s.commit()` 之后引用 `doc.id`/`cand.id`；dedup 提前返回时 RawDocument 已提交（extraction 路径）或未创建（direct 路径），两种情形均安全。

验收：`python -m pytest tests/test_features.py tests/test_e2e.py -q` 全过（31 个）。

### F7 scheduler 增加 coalesce idle 冲刷 job（P1，T04）

文件：`remembrance/core/scheduler.py`，在 `start_scheduler()` 的 forgetting job 之后、`scheduler.start()` 之前追加：

```python
    # coalesce idle flush（每 2 秒检查一次空闲缓冲）
    if settings.COALESCE_ENABLED:
        from remembrance.services.memory_service import coalesce_buffer
        scheduler.add_job(coalesce_buffer.check_idle, "interval",
                          seconds=2, id="coalesce_idle", replace_existing=True)
```

（import 放在函数内部避免模块级循环依赖。）

验收：`python -c "from remembrance.core.scheduler import start_scheduler; start_scheduler(); print('scheduler OK')"` 输出正常；`python -m pytest tests/test_features.py -q` 全过。

### F8 /stats 增加 worker 上次运行时间（P1，US-20）

**步骤 1**——`remembrance/core/scheduler.py` 顶部（`scheduler = ...` 之后）追加：

```python
WORKER_LAST_RUN: dict[str, str] = {}


def record_run(name: str) -> None:
    from remembrance.core.time import utcnow
    WORKER_LAST_RUN[name] = utcnow().isoformat()
```

**步骤 2**——三个 worker 各在 `run()` 末尾（`return` 之前）记录：`remembrance/workers/ingest_worker.py` 的 `run_ingest()` 末尾加 `scheduler_mod.record_run("ingest")`；`evolve_worker.py` 的 `run_evolve()` 末尾加 `record_run("evolve")`；`forgetting_worker.py` 的 `run_forgetting()` 末尾加 `record_run("forgetting")`。import 方式统一为 `from remembrance.core import scheduler as scheduler_mod`（workers 已 import scheduler 模块的沿用现有别名）。

**步骤 3**——`remembrance/api/routes_health.py` 的 `stats()` 返回 dict 追加：

```python
        "workers": scheduler.WORKER_LAST_RUN,
```

验收：`python -m pytest tests/test_features.py::TestHealthStats -q` 全过（3 个）。

**Wave 2 提交**: `git commit -am "fix(v0.3.1): fastpath chain via evolve_worker in_, wire dedup three-state, coalesce idle job, /stats worker times"`

---

## Wave 3：集成修复（F9–F12）

### F9 shell_hook 2s 硬超时（P1，ADR-0006 核心承诺）

文件：`scripts/shell_hook.py`，整体替换为以下结构（保留既有的 build_context/main 逻辑，仅包超时）：

```python
"""Shell Hook: pre_llm_call 时注入相关记忆（零依赖 CLI）。
契约：stdin JSON → stdout {context} 或 {}；2s 硬超时；异常静默降级。"""
import json
import os
import sys
from concurrent.futures import ThreadPoolExecutor, TimeoutError as FuturesTimeout

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from remembrance.core.settings import settings
from remembrance.core.time import utcnow
from remembrance.llm.client import embed
from remembrance.storage.vector_store import vector_store


def build_context(query: str) -> dict:
    """查询相关记忆，构建注入上下文。"""
    if not query or len(query.strip()) <= settings.SHELL_HOOK_MIN_CHARS:
        return {}

    try:
        query_vec = embed([query])[0]
        results = vector_store.search(query, top_k=settings.SHELL_HOOK_TOP_K)
        # ...（保持既有实现：active 过滤、decay 计算、MIN_SCORE 过滤、Markdown 列表构建，逐行保留原文件 28-63 行逻辑）
    except Exception:
        return {}
```

然后 `main()` 改为：

```python
def main():
    raw = sys.stdin.read().strip()
    if not raw:
        print("{}")
        return

    try:
        data = json.loads(raw)
        query = data.get("query", "") or data.get("message", "") or data.get("prompt", "")
    except json.JSONDecodeError:
        print("{}")
        return

    with ThreadPoolExecutor(max_workers=1) as pool:
        future = pool.submit(build_context, query)
        try:
            result = future.result(timeout=settings.SHELL_HOOK_TIMEOUT)
        except FuturesTimeout:
            print("{}")
            os._exit(0)  # 硬退出：不等滞留线程，保证宿主不被拖慢
        except Exception:
            result = {}
    print(json.dumps(result, ensure_ascii=False))


if __name__ == "__main__":
    main()
```

注意：`build_context` 的函数体除签名外**逐行保留原实现**（vector 检索、active 过滤、decay、`- [{score}] {content}` 列表）；`ThreadPoolExecutor` 用 `with` 包裹但超时分支走 `os._exit(0)` 绕过 join 等待。

验收：`echo {"query":"什么是混合检索"} | python scripts/shell_hook.py` 在 3 秒内退出且输出合法 JSON（`{}` 或 `{"context":...}`）；`python -c "import json; json.loads(open('nul').read() if False else '{}')"` 类格式自检非必须。

### F10 mcp_server 升级为标准 MCP 协议（P1，US-34）

文件：`scripts/mcp_server.py`。保留 `API = ...`、`call_api()`、`TOOLS` 三工具定义（inputSchema 原样保留），重写 stdio 循环为标准 MCP JSON-RPC 2.0：

```python
PROTOCOL_VERSION = "2024-11-05"

TOOL_DESCRIPTIONS = {
    "search":   {"description": "搜索记忆", "inputSchema": TOOLS["search"]["inputSchema"]},
    "add":      {"description": "添加记忆", "inputSchema": TOOLS["add"]["inputSchema"]},
    "feedback": {"description": "反馈记忆有用性", "inputSchema": TOOLS["feedback"]["inputSchema"]},
}


def handle(msg: dict) -> dict | None:
    mid = msg.get("id")
    method = msg.get("method", "")
    if method == "initialize":
        return {"jsonrpc": "2.0", "id": mid, "result": {
            "protocolVersion": PROTOCOL_VERSION,
            "capabilities": {"tools": {}},
            "serverInfo": {"name": "remembrance", "version": "0.3.1"}}}
    if method == "notifications/initialized":
        return None  # 通知无响应
    if method == "ping":
        return {"jsonrpc": "2.0", "id": mid, "result": {}}
    if method == "tools/list":
        return {"jsonrpc": "2.0", "id": mid, "result": {
            "tools": [{"name": n, **meta} for n, meta in TOOL_DESCRIPTIONS.items()]}}
    if method == "tools/call":
        params = msg.get("params", {})
        name = params.get("name", "")
        args = params.get("arguments", {})
        if name not in TOOLS:
            return {"jsonrpc": "2.0", "id": mid,
                    "error": {"code": -32602, "message": f"unknown tool: {name}"}}
        result = call_api(TOOLS[name]["method"], TOOLS[name]["path"], args)
        return {"jsonrpc": "2.0", "id": mid, "result": {
            "content": [{"type": "text", "text": json.dumps(result, ensure_ascii=False)}]}}
    return {"jsonrpc": "2.0", "id": mid,
            "error": {"code": -32601, "message": f"method not found: {method}"}}


def main():
    for line in sys.stdin:
        line = line.strip()
        if not line:
            continue
        try:
            msg = json.loads(line)
        except json.JSONDecodeError:
            continue
        resp = handle(msg)
        if resp is not None:
            sys.stdout.write(json.dumps(resp, ensure_ascii=False) + "\n")
            sys.stdout.flush()
```

验收（一体化冒烟）：`printf '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}\n{"jsonrpc":"2.0","id":2,"method":"tools/list"}\n' | python scripts/mcp_server.py` 输出两行 JSON，第一行 result.serverInfo.name == "remembrance"，第二行 result.tools 长度 3。（PowerShell 下用 `"..." | python scripts/mcp_server.py` 等价。）

### F11 创建 scripts/upgrade_check.py（P1，US-45、T14）

新建文件 `scripts/upgrade_check.py`：

```python
"""升级前检查：版本、数据目录、DB 表结构、向量库路径。退出码 0=可升级，1=有问题。"""
import os
import sqlite3
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from remembrance.core.settings import settings

REQUIRED_TABLES = [
    "rawdocument", "memorycandidate", "memoryitem", "memoryedge",
    "memoryproposal", "memorycheckpoint", "ingestsource",
    "evolutionfeedback", "documentchunk",
]


def main() -> int:
    issues: list[str] = []

    print(f"[upgrade-check] REMEMBRANCE_HOME = {settings.REMEMBRANCE_HOME}")
    if not settings.REMEMBRANCE_HOME.exists():
        issues.append("REMEMBRANCE_HOME 不存在")

    db_url = settings.database_url  # sqlite:///C:/...
    db_path = db_url.replace("sqlite:///", "")
    if os.path.exists(db_path):
        conn = sqlite3.connect(db_path)
        existing = {r[0] for r in conn.execute(
            "SELECT name FROM sqlite_master WHERE type='table'")}
        conn.close()
        missing = [t for t in REQUIRED_TABLES if t not in existing]
        if missing:
            issues.append(f"DB 缺表: {', '.join(missing)}")
        print(f"[upgrade-check] DB 表: {len(existing)} 张，缺失 {len(missing)} 张")
    else:
        print("[upgrade-check] DB 文件不存在（首次启动将自动创建）")

    if not settings.chromadb_path.exists():
        print("[upgrade-check] chromadb 目录不存在（首次启动将自动创建）")

    if not os.environ.get("OPENAI_API_KEY"):
        issues.append("OPENAI_API_KEY 未设置（LLM/Embedding 将不可用）")

    if issues:
        print("[upgrade-check] 发现问题:")
        for i in issues:
            print(f"  - {i}")
        return 1
    print("[upgrade-check] 通过，可以升级")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

验收：`python scripts/upgrade_check.py` 退出码为 0 或 1 且输出以 `[upgrade-check]` 开头的逐行报告。

### F12 restore.py 增加停服保护（P2，US-43）

文件：`scripts/restore.py`，`import` 区追加 `import argparse, json, urllib.request`，`main()` 开头改为：

```python
def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("backup_dir", nargs="?", default=None)
    parser.add_argument("--force", action="store_true", help="服务运行中也强制恢复")
    args = parser.parse_args()

    # 停服保护：服务在线时拒绝热恢复（SQLite/Chroma 写入竞争）
    try:
        with urllib.request.urlopen(
                f"http://localhost:{settings.port}/health", timeout=1) as r:
            if r.status == 200 and not args.force:
                print("[restore] 服务正在运行，请先停止服务再恢复（或加 --force）")
                return 1
    except Exception:
        pass  # 服务未运行，安全

    backup_dir = Path(args.backup_dir) if args.backup_dir else find_latest_backup()
```

（原 `main()` 的参数解析行删除。）

验收：`python scripts/restore.py --help` 输出含 `--force`。

**Wave 3 提交**: `git commit -am "fix(v0.3.1): shell_hook 2s hard timeout, standard MCP protocol, upgrade_check.py, restore stop-guard"`

---

## Wave 4：架构收尾（F13）

### F13 checkpoint/edges 路由下沉 service 层（P2，ADR-0001 彻底合规）

**步骤 1**——`remembrance/services/evolution_service.py` 追加：

```python
def list_checkpoints(memory_id: str, limit: int = 20) -> dict:
    with db.get_session() as s:
        rows = s.exec(select(MemoryCheckpoint)
                      .where(MemoryCheckpoint.memory_id == memory_id)
                      .order_by(MemoryCheckpoint.version.desc())
                      .limit(limit)).all()
        return {"checkpoints": [r.model_dump(mode="json") for r in rows]}
```

（文件顶部 import 追加 `MemoryCheckpoint`。）

**步骤 2**——新建 `remembrance/services/edge_service.py`，将 `routes_edges.py` 的 create/list/chain/delete 四个操作原样搬入（函数签名：`add_edge(source_id, target_id, relation, confidence)`、`list_edges(memory_id, relation)`、`get_chain(memory_id)`、`remove_edge(edge_id) -> bool`），存储调用保持 `remembrance.storage.edges` 的现有函数。

**步骤 3**——`routes_checkpoint.py` 改为：

```python
from fastapi import APIRouter
from remembrance.services.evolution_service import list_checkpoints

router = APIRouter()


@router.get("/checkpoint")
def list_checkpoints_route(memory_id: str, limit: int = 20):
    return list_checkpoints(memory_id, limit)
```

`routes_edges.py` 改为薄 handler（保留 `EdgeReq` 模型——它属于 HTTP 层契约，允许留在路由文件），CRUD 调用 `edge_service`；`remove_edge` 返回 False 时 handler 抛 `HTTPException(404)`。

验收：`python -m pytest tests/test_e2e.py -q` 全过（18 个）。

**Wave 4 提交**: `git commit -am "refactor(v0.3.1): sink checkpoint/edges logic to service layer"`

---

## Wave 5：测试补强（F14–F17）

### F14 修复空壳测试 test_search_with_trace（P2）

文件：`tests/test_features.py` 第 135–141 行 `test_search_with_trace`，替换函数体断言部分为：

```python
        resp = client.post("/search", json={"query": "向量检索", "trace": True})
        assert resp.status_code == 200
        data = resp.json()
        assert "trace" in data
        steps = {s["step"] for s in data["trace"]}
        assert "prefilter" in steps
```

### F15 新建 tests/test_dedup.py（P1 配套）

新建文件，完整内容：

```python
"""三态去重（dedup）测试：merge / update / insert。"""
import pytest
from fastapi.testclient import TestClient
from sqlmodel import SQLModel, create_engine, Session
from sqlmodel.pool import StaticPool

from remembrance.storage import db
from remembrance.models.tables import MemoryItem
from remembrance.core.time import utcnow


@pytest.fixture(name="client")
def client_fixture(monkeypatch):
    engine = create_engine("sqlite:///:memory:", connect_args={"check_same_thread": False},
                           poolclass=StaticPool)
    db.engine = engine
    SQLModel.metadata.create_all(engine)

    def mock_search(content_or_vec, top_k=8, where=None):
        # 由测试体通过 db.engine 预置的 MemoryItem 控制：默认返回空（insert 场景）
        return getattr(mock_search, "results", [])

    monkeypatch.setattr("remembrance.services.memory_service.vector_store.search", mock_search)
    monkeypatch.setattr("remembrance.parsing.extractor.extractor.chat_json",
                        lambda *a, **kw: {"title": "t", "content": "c", "lane": "default"})

    from api_server import app
    with TestClient(app) as c:
        c.mock_search = mock_search
        yield c


def _seed_memory() -> str:
    with Session(db.engine) as s:
        mem = MemoryItem(title="偏好", content="用户喜欢 coffee", lane="preference",
                         category="preference", status="active", importance=0.5)
        s.add(mem)
        s.commit()
        s.refresh(mem)
        return mem.id


def test_dedup_merge(client):
    mem_id = _seed_memory()
    client.mock_search.results = [{"id": mem_id, "document": "用户喜欢 coffee",
                                   "metadata": {}, "distance": 0.1}]  # sim=0.9 ≥ 0.80
    resp = client.post("/add", json={"title": "x", "content": "用户喜欢喝咖啡"})
    assert resp.status_code == 200
    data = resp.json()
    assert data.get("dedup_action") == "merge"
    assert data.get("target_memory_id") == mem_id


def test_dedup_update(client):
    mem_id = _seed_memory()
    client.mock_search.results = [{"id": mem_id, "document": "用户喜欢 coffee",
                                   "metadata": {}, "distance": 0.3}]  # sim=0.7 ∈ [0.65, 0.80)
    resp = client.post("/add", json={"title": "x", "content": "用户最近爱喝拿铁"})
    assert resp.status_code == 200
    data = resp.json()
    assert data.get("dedup_action") == "update"
    assert "proposal_id" in data


def test_dedup_insert(client):
    client.mock_search.results = []  # 无相似 → insert
    resp = client.post("/add", json={"title": "x", "content": "全新的记忆内容条目"})
    assert resp.status_code == 200
    data = resp.json()
    assert "dedup_action" not in data
    assert "candidate_id" in data
```

### F16 forgetting archived 测试（P1 配套）

文件：`tests/test_features.py` 新增类（文件末尾追加）：

```python
class TestForgettingArchived:
    def test_decay_below_threshold_auto_archived(self, client):
        from datetime import timedelta
        from remembrance.memory.forgetting import compute_decay
        from remembrance.models.tables import MemoryItem
        from remembrance.core.time import utcnow
        from sqlmodel import Session, select
        from remembrance.storage import db as db_mod

        with Session(db_mod.engine) as s:
            old = MemoryItem(
                title="旧工作记忆", content="临时事项已完成", lane="working",
                category="general", status="active",
                importance=0.1, access_count=0,
                decay_score=0.2,  # 低于阈值 0.3
                last_accessed=utcnow() - timedelta(days=100),
                created_at=utcnow() - timedelta(days=100))
            s.add(old)
            s.commit()
            s.refresh(old)
            mid = old.id

        archived = compute_decay()
        assert mid in archived
        with Session(db_mod.engine) as s:
            mem = s.get(MemoryItem, mid)
            assert mem.status == "archived"

    def test_search_excludes_archived(self, client):
        # hybrid.py 的 SQL 过滤验证：构造 archived 记忆不影响 WHERE 子句存在性
        import inspect
        from remembrance.retrieval import hybrid
        src = inspect.getsource(hybrid)
        assert '.where(MemoryItem.status == "active")' in src
```

### F17 shell_hook 超时测试（P1 配套）

新建 `tests/test_shell_hook.py`：

```python
"""Shell Hook 契约测试：超时与静默降级。"""
import importlib.util
import json
import os
import time

import pytest

HOOK_PATH = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))),
                         "scripts", "shell_hook.py")


def _load_hook(monkeypatch, embed_delay=0.0, timeout=0.2):
    monkeypatch.setenv("SHELL_HOOK_TIMEOUT", str(timeout))
    spec = importlib.util.spec_from_file_location("shell_hook", HOOK_PATH)
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)

    def slow_embed(texts):
        time.sleep(embed_delay)
        return [[0.1] * 8]

    monkeypatch.setattr(mod, "embed", slow_embed)
    return mod


def test_build_context_short_query_returns_empty(monkeypatch, capsys):
    mod = _load_hook(monkeypatch)
    assert mod.build_context("你好") == {}


def test_main_timeout_returns_empty_json(monkeypatch, capsys):
    mod = _load_hook(monkeypatch, embed_delay=5.0, timeout=0.2)
    import io
    monkeypatch.setattr("sys.stdin", io.StringIO(json.dumps({"query": "这是一个超过三字符的查询"})))
    start = time.perf_counter()
    with pytest.raises(SystemExit):
        mod.main()
    elapsed = time.perf_counter() - start
    out = capsys.readouterr().out.strip()
    assert json.loads(out) == {}
    assert elapsed < 2.0  # 硬超时生效（远小于 embed 的 5s）
```

注：`os._exit(0)` 在测试中表现为 `SystemExit`——若实测未被 pytest 捕获，则将断言改为 `pytest.raises((SystemExit, Exception))` 并仅校验输出与耗时。

**Wave 5 验收与提交**: `python -m pytest tests/ -q --tb=short`，期望 **≥93 passed / 6 failed**（6 个预存失败不变）。`git commit -am "test(v0.3.1): dedup/forgetting/shell_hook tests, fix hollow trace test"`

---

## Wave 6：文档与票据状态修正（F18）

### F18 修正结果文档与票据状态（P1 流程真实性）

**步骤 1**——`docs/aidumem-port-results.md`：

- 测试表更新为实际值：`test_settings.py` 14、`test_infra.py` 5、`test_features.py` 15（13+2 新增 forgetting）、`test_dedup.py` 3、`test_shell_hook.py` 2、`test_e2e.py` 18、`test_auth.py` 6、`test_prefilter.py` 16、`test_p0.py` 7、`test_reranker.py` 8，**总计 94**；表下注明"6 个失败为 v0.2.0 预存 bug（test_p0 4 + test_prefilter 2），经基线 8e0d249 复现确认"。
- "新增/修改 Python 模块 22 个"改为"44 个（git diff 2966b3d..HEAD 非测试 .py 实测）"。
- 架构图 `scripts/` 一节补上 `upgrade_check.py`。
- 文末追加一节 `## v0.3.1 审计修复`，列出本方案 Wave 1–5 的 commit 号（执行时填入实际 hash）。

**步骤 2**——`.scratch/aidumem-impl/issues/` 下 04–14 共 11 张票据：`Status: ready-for-agent` → `Status: resolved`，各文件末尾追加：

```markdown
## Answer

代码已于 e18e273 提交；验收缺口（idle 冲刷调用方/dedup 接线/超时/样本数/upgrade_check 等）由 v0.3.1 审计修复方案补齐，见 docs/plans/v0.3.1-audit-remediation-plan.md。
```

**步骤 3**——`CONTEXT.md` 词汇表更新：`candidate` 条目的状态值补充 `fastpath`；新增 `water_level` 条目（coalesce 缓冲水位，用于 /stats）。

**Wave 6 提交**: `git commit -am "docs(v0.3.1): correct results doc numbers, resolve tickets 04-14, update glossary"`

---

## Wave 7：终验

1. 全新 venv 克隆即跑验证：`python -m venv .venv-verify && .venv-verify\Scripts\python.exe -m pip install -q -e . && .venv-verify\Scripts\python.exe -c "from remembrance.retrieval import hybrid; import api_server; print('clone-and-run OK')"`——必须输出 `clone-and-run OK`（F1 的终验）。
2. 全量测试：`python -m pytest tests/ -q --tb=short`——**94 个测试中 ≥88 passed、恰好 6 failed（均为预存）**。
3. `git log --oneline -8` 确认 6 个 Wave 提交齐全。
4. 向用户输出执行报告：各 Wave 状态（DONE/FAILED/SKIPPED + commit hash）、最终测试数字、残留事项。提示用户手动删除 `.venv-audit/`、`.venv-verify/`、`.audit-baseline/`（递归删除被安全策略禁止，需用户操作）。

---

## 附：本方案不做的事（防止执行者越界）

- 不修 6 个预存测试失败（属 v0.2.0 技术债，另立票据处理）。
- 不引入 Qdrant/mem0（ADR-0004 已定案）。
- 不改 `_lane_strength` 数值、不动 gate 五档决策逻辑。
- 不为 DocumentChunk 死代码做任何处理（预存观察项，非本次范围）。
