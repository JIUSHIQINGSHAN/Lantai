# 对话压缩：吸收借鉴aiduMEI（2026-08-10 → 08-12）

> 压缩对象：Codex 会话「吸收借鉴aiduMEI」（id 019feab6-b27d-7d11-b10c-70ff28489fcd，5794 行记录，7 次自动压缩检查点）。
> 压缩方式：借鉴 aiduMEI on_pre_compress 思路——压缩前先把要点落盘，不丢信息；产物可整段粘入新会话继续。

## 会话概况

- 起点：用户提示借鉴作者 monkey2jack/aiduMEI 更新，要求审阅并继续吸收（项目「兰台记忆」）。
- 主线：以「借鉴吸收」赛道连续交付 v0.6 → v0.13 共 8 波（每波含代码 + 不 mock 冒烟测试 + 票据收口 + CHANGELOG/词汇表登记 + 服务重启验证 + 提交推送）。
- 收尾：v0.13 星图防重叠 + 中国色换肤后，用户转入 UI 审美调研，对话在「推荐 5 个具体风格 + 预览网页版」处被中断。

## 主线产出（按波次）

| 波次 | 内容 | 关键提交 |
|---|---|---|
| v0.6 收口 | DB 版本化迁移（PRAGMA user_version，v2）；VAULT 档案控制台（/ui/vault、GET /memories、/stats by_decay_class）；冷启动导入 verbatim 直存 POST /import/jsonl，与并行会话 L0 会话链合并为双通道（ADR-0018）；lane 级 ACL（AGENT_LANE_BINDINGS 空=禁用、X-Agent-Id） | beb29ef 7152e33 |
| MCP 扩容 | 工具数 21→28→34→38→39：mem_recent/mem_stats/mem_health/autodream_*、proposals_list/proposal_decide、tree_*、crystals_*、reflect_run/mem_usage/core_memory_get/verbatim_search、graph_view；抽取 lantai/ops/usage.py::collect_usage 共用 | c42c653 a2fea07 |
| v0.7 树状图谱+技能结晶 | MemoryNode + memoryitem.tree_path（v9 迁移）；tree_service/crystal_service + REST + MCP | 9d88d0c |
| v0.9 MAP 记忆星图 | lantai/ops/graph.py::build_graph(limit=150) 纯函数；GET /graph；MCP graph_view；/ui/map 零外部依赖内联 SVG（6 lane 扇区、scene 聚簇、四色边 supports 绿/refines 蓝/contradicts 橙/supersedes 红） | 6f1c99d + code-review 收口 fe16a19 c948a17 |
| v0.10 目识 Vision | vision_caption(media_url)、AddMemoryReq.media_url、validate_media_url 白名单、vision 溯源（provenance_prompt=vision-caption）；中文名「目识」先登记 CONTEXT/ADR-0013 | b302d57 |
| v0.11 烽燧广播链 | 记忆广播链（借鉴 aiduMEI memory_broadcast） | ab7ed5c |
| v0.12 截屏入忆 | scripts/screenshot_memory.ps1（剪贴板截图→data URI→POST /add）；MEDIA_DATA_URI_MAX_BYTES=10MB；data URI 严格校验（MIME 白名单 png/jpeg/webp/gif + base64 严格解码 + 上限）；media_url max 15,000,000 | d6469b9 |
| v0.13 书卷换肤 | 星图防重叠布局重写（1800×1300、比例分槽、角度排序+最小 4° 贪心、标签描边）+ 全局中国色（zhongguose 全谱 526 色，书卷水墨风） | aa3ae10（feat(ui)） |
| v0.14 双主题换肤 | 五式预览（玄墨/天青/书衣/吉金/漏窗，`.scratch/v0.14-style-preview/`）→ 用户选定吉金+漏窗 → `routes_ui.py` 六个面板全局双主题（默认吉金 + `[data-theme=louchuang]` 漏窗，右上切换钮 + `?theme=` 深链，星图配色读 CSS 变量随主题重绘）；两式名登记 CONTEXT/ADR-0013 | 本次提交（feat(ui)） |

## 关键决策与红线（均已登记）

- 宁 miss 不脏写：校验失败不静默丢弃/不自动修正，进 pending_review 交用户裁决。
- 明确不吸收：装饰性登录门禁、回声室提权、无测试照搬、Code Graph/IDE 钩子、layer1_selfcheck 容量>80% 自动合并、instinct_graduation 自动毕业删原文。
- 命名纪律：中文名（目识/星图/烽燧等）先登记 CONTEXT.md + ADR-0013 再使用。
- 测试纪律：核心函数必须有真实 SQLite+FTS 不 mock 冒烟测试；只 mock LLM/embedding/向量存储。
- 零外部依赖：UI 面板内联 HTML/SVG，不用 CDN。
- 提交规范：吸收赛道 feat(absorb) + 中文说明；UI 用 feat(ui)；票据收口写 resolved（随提交推送）；CHANGELOG 插 Unreleased 顶部；只 add 自己的文件（并行会话遗留勿动）。

## 当前状态（2026-08-12，v0.14 收尾后）

- 本次提交入库：v0.14 吉金/漏窗双主题换肤 + 命名登记（CONTEXT/ADR-0013）+ CHANGELOG + 本压缩稿；遗留 scratch（convert_reflexion.py、manual_reflect_trigger.py、reflexion-2303.11366.html、docs/memory-digest/）按用户指示一并提交。
- 服务：127.0.0.1:8767（.venv-verify 启动，日志 .scratch/v0.14-style-preview/server.log / server.err.log，*.log 已 gitignore）。
- 反思/reflect 线（多为并行会话产出）：观察期保底、运行落库 v8-v10、decision_reason、校准五分类等已推送。

## 未完成 / 待续

1. UI 审美调研（对话中断处）：用户要求「调研各渠道类似界面 → 结合中国传统文化制定计划 → 推荐 5 个更具体风格 + 预览网页版」，尚未交付；用户会亲自把关审美。
1. ~~UI 审美调研~~ ✅ 已完成（2026-08-12）：五式预览 → 用户选定吉金+漏窗 → 双主题换肤落地，详见「主线产出」v0.14 行与 `.scratch/v0.14-style-preview/spec.md`。
2. 演示数据（2 条样例记忆）是否删除，待用户确认。
3. ACL 的 MCP 侧 agent 身份传递（X-Agent-Id 不适用于 MCP 工具，票据 08 已注明）。

## 环境约束与工作方式

- 所有命令必须 require_escalated + 合理 prefix_rule；审批 503 时等 15-30s 重试同一命令，勿绕路。
- 虚拟环境 .venv-verify\Scripts\python.exe（系统 Python312 缺 sqlmodel）；apply_patch 在此环境不可用（WindowsApps 权限），文件编辑用 heredoc Python 脚本 + 断言防误改。
- 完整 pytest 可能挂起，用相关子集或流式跑（最近全量 616 passed）；测试夹具：patch lantai.storage.db.get_session 到内存 SQLite+FTS。
- 服务重启：先结束 8767 旧进程（确认是 api_server），再隐藏窗口启动新进程并分开重定向日志，等 8-10s 后验证 /ui/map 200。

## 关键参考

- aiduMEI 浅克隆：临时目录 aidumei-inspect（作者 08-11 停在 v18.3.0）；白皮书 Desktop\文档\aiduMEM-v10.1-Clotho-Whitepaper.md 与 aiduMEM-v11-Hyperion-Whitepaper.md。
- aiduMEI 压缩机制（可继续借鉴）：Hermes 插件 on_pre_compress 压缩前把轮次落盘 /add；/facts/compress 返回压缩率；consolidator 24h 后台合并。
- 换肤色源：zhongguose.com/colors.json（526 色，已存临时目录 zhongguose-colors.json）。
- 星图效果图：.codex\visualizations\2026\08\10\019feab6-b27d-7d11-b10c-70ff28489fcd\lantai-map-v3b.png。
- 吸收赛道计划/票据：.scratch/v0.6-aidumei-absorb/spec.md；后续各 v0.x-* 目录；TencentDB 借鉴报告 docs/research/tencentdb-agent-memory-borrow.md。
